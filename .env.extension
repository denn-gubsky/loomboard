# Loaded by `vite --mode extension` (the Chrome MV3 extension build). Marks the
# extension build: no same-origin proxy — the side panel fetches the absolute
# loomcycle base URL directly with a plain fetch + bearer (the manifest's
# host_permissions bypass page CORS). Non-secret build flag.
VITE_LOOMBOARD_EXTENSION=true
