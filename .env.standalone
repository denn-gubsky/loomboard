# Loaded by `vite build --mode standalone` (the CLI's SPA build). Enables
# proxy mode in a production build so the app routes /v1 through the CLI's
# reverse proxy (loomcycle sends no CORS headers). Non-secret build flag.
VITE_LOOMBOARD_STANDALONE=true
